# Configuración básica
spring.application.name=market-app
spring.profiles.active=dev
server.port=8090
server.servlet.context-path=/market-app/api

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://localhost:5432/market-3a
spring.datasource.username=postgres
spring.datasource.password=Mariana2006?
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
security.jwt.secret=miClaveSecretaSuperSeguraParaJWTQueDebeSerMuyLargaYComplejaConAlMenos256Bits12345
security.jwt.expiration=86400000

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Logging
logging.level.com.tecdesoftware.market=DEBUG
logging.level.org.springframework.security=DEBUG